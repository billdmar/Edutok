/// LeaderboardWrapper.swift
///
/// Wrapper view that hosts the LeaderboardView.
import SwiftUI

struct LeaderboardWrapper: View {
    var body: some View {
        ZStack {
            // Leaderboard content
            LeaderboardView()
        }
    }
}
